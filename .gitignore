__pycache__/
*.pyc

# Regenerable run output. Figures are tracked because the README embeds them.
sweep.csv
sweep.png

# LaTeX byproducts. The PDF itself is tracked, since it is the deliverable and
# nobody should need a TeX installation to read it.
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.synctex.gz
paper/*.fls
paper/*.fdb_latexmk
explainer/*.aux
explainer/*.log
explainer/*.out
explainer/*.toc
explainer/*.synctex.gz
explainer/*.fls
explainer/*.fdb_latexmk
